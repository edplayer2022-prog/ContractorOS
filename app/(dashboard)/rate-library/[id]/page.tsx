import { notFound } from "next/navigation";import { RateForm } from "@/components/rate-form";import { PageHeader } from "@/components/ui";import { getAuthContext } from "@/lib/data";export default async function EditRatePage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase,company}=await getAuthContext();const{data:item}=await supabase.from("rate_library").select("*").eq("id",id).single();if(!item)notFound();return <><PageHeader title="Edit Service" description="Update default service costs and pricing."/><RateForm company={company!} item={item}/></>}
