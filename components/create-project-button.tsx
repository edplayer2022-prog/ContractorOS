"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {FolderPlus} from "lucide-react";import {toast} from "sonner";import {Button} from "@/components/ui";import {createClient} from "@/lib/supabase/client";
export function CreateProjectButton({estimateId}:{estimateId:string}){const[loading,setLoading]=useState(false);const router=useRouter();async function create(){setLoading(true);const{data,error}=await createClient().rpc("create_project_from_estimate",{target_estimate_id:estimateId});setLoading(false);if(error){toast.error(error.message);return}toast.success("Project created.");router.push(`/projects/${data}`);router.refresh()}return <Button onClick={create} loading={loading}><FolderPlus className="h-4 w-4"/>Create Project</Button>}
