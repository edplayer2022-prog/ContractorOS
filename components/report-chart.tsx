import{currency}from"@/lib/utils";
export function BarChart({data,valueKey="revenue",empty="No activity in this period."}:{data:{month:string;revenue:number;payments:number}[];valueKey?:"revenue"|"payments";empty?:string}){if(!data.length)return <p className="py-10 text-center text-sm text-slate-500">{empty}</p>;const max=Math.max(...data.map(item=>item[valueKey]),1);return <div className="flex h-52 items-end gap-3 overflow-x-auto pt-8">{data.map(item=><div key={item.month} className="flex min-w-16 flex-1 flex-col items-center gap-2"><span className="text-[10px] font-semibold text-slate-500">{currency(item[valueKey])}</span><div className="w-full max-w-20 rounded-t-md bg-orange-500" style={{height:`${Math.max(4,item[valueKey]/max*130)}px`}}/><span className="text-xs text-slate-500">{item.month}</span></div>)}</div>}
export function Distribution({items}:{items:{label:string;count:number;value:number}[]}){const total=items.reduce((s,i)=>s+i.count,0)||1;return <div className="grid gap-3">{items.map(item=><div key={item.label}><div className="mb-1 flex justify-between text-sm"><span className="capitalize">{item.label}</span><span>{item.count} · {currency(item.value)}</span></div><div className="h-2 overflow-hidden rounded bg-slate-100"><div className="h-full rounded bg-orange-500" style={{width:`${item.count/total*100}%`}}/></div></div>)}</div>}
