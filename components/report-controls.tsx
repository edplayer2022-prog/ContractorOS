import Link from"next/link";import{Download}from"lucide-react";import{Button}from"@/components/ui";import{PrintReportButton}from"@/components/print-report-button";import type{DateRange}from"@/lib/reporting";
const presets=[['this_month','This Month'],['last_month','Last Month'],['last_30_days','Last 30 Days'],['last_90_days','Last 90 Days'],['this_year','This Year'],['last_year','Last Year'],['custom','Custom Range']];
export function DateFilter({range,section,showExport}:{range:DateRange;section?:string;showExport?:string}){const query=new URLSearchParams({period:range.preset,from:range.from,to:range.to,...(section?{section}:{})});return <div className="no-print mb-6 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><form className="flex flex-col gap-3 lg:flex-row lg:items-end"><label className="grid gap-1 text-sm font-semibold text-slate-700"><span>Period</span><select name="period" defaultValue={range.preset} className="h-11 rounded-lg border border-slate-300 bg-white px-3">{presets.map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></label><label className="grid gap-1 text-sm font-semibold text-slate-700"><span>From</span><input name="from" type="date" defaultValue={range.from} className="h-11 rounded-lg border border-slate-300 px-3"/></label><label className="grid gap-1 text-sm font-semibold text-slate-700"><span>To</span><input name="to" type="date" defaultValue={range.to} className="h-11 rounded-lg border border-slate-300 px-3"/></label>{section&&<input type="hidden" name="section" value={section}/>}<Button>Apply</Button><div className="flex flex-1 justify-end gap-2">{showExport&&<Link href={`/api/reports/export?kind=${showExport}&${query}`}><Button type="button" variant="secondary"><Download className="h-4 w-4"/>CSV</Button></Link>}<PrintReportButton/></div></form></div>}
